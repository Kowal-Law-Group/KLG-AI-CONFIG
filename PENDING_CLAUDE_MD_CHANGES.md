# Pending claude.md changes

This file collects every backlog item whose fix is "add/change a rule in
claude.md." The real `claude.md` landed 2026-08-17 (see README STATUS),
so this file is now empty — both entries that were staged here (Westlaw
Find & Print global rule, never-CC-clients-on-emails-to-opposing-counsel)
were applied directly to `claude.md` in that same pass. See CHANGELOG.md
for the version that landed them.

Each entry going forward: source backlog link, proposed section, exact
text to add, scope/applies-to notes — same format as before, applied
directly to `claude.md` rather than staged here, since the real file now
exists and there's no reason to defer.

---

*(Add new entries below this line only if a future claude.md-rule backlog
item needs to be staged for some reason — e.g., a batch of several landing
together as one reviewed commit.)*
